class subCategory {
    constructor(id, categoryId, title, color) {
        this.id = id;
        this.categoryId = categoryId;
        this.title = title;
        this.color = color;
    }
}

export default subCategory;