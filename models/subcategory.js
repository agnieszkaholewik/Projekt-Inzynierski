class subCategory {
    constructor(id, categoryId, title, color, icon) {
        this.id = id;
        this.categoryId = categoryId;
        this.title = title;
        this.color = color;
        this.icon=icon;
    }
}

export default subCategory;